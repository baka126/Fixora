apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fixora.fullname" . }}-test-connection"
  labels:
    {{- include "fixora.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fixora.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
